{% extends "layout.njk" %}

{% block content %}

<section class="shop container">
    <h2>Últimos Produtos</h2>

    <div class="items">

        {%for product in products%}
            <div class="product">
                <div
                    class="image"
                    style=                    
                    >
                    
                    <p>{{product.price}}</p>
                    <h4>{{product.name}}</h4>
                    
                    <a href="/products/{{product.image}}"></a>
                    
                    </div>

            </div>
        {%endfor%}
    </div>
</section>

{% endblock %}

{# comment #}